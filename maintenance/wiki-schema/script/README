createDB dropDB and loadSchema scripts take as input the user and password of the local mysql installation, and create drop and load the schemata of all the wikipedia versions available in the benchmark (164 at the time i'm writing)
The user must be enable to create and drop databases, root most probably.
